function cutInHalfAndFloor(num) {
  let newNum = Math.floor(num / 2);  //creates a new variable and sets it equal to the passed number divided by 2, and rounded down if necessary
  return newNum;   //returns the new number
}

// Do not edit this line;
module.exports = cutInHalfAndFloor;
